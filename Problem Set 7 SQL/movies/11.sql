SELECT DISTINCT m.title FROM people AS p JOIN stars AS s ON p.id = s.person_id JOIN movies AS m ON s.movie_id = m.id JOIN ratings AS r ON r.movie_id = m.id  WHERE p.name = 'Chadwick Boseman' ORDER BY r.rating DESC LIMIT 5;
